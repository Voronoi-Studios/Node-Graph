{
  "name": "node-graph",
  "version": "0.1.0",
  "description": "Framework-agnostic Web Component for embedding the node graph, distributed via jsDelivr off GitHub tags.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "serve": "npx http-server dist -p 8080 -c-1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
